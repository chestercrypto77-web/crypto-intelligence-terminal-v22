name: V22 15-Minute Free Validation

on:
  workflow_dispatch:
  schedule:
    - cron: "7,22,37,52 * * * *"

permissions:
  contents: read

concurrency:
  group: v22-free-15m-validation
  cancel-in-progress: false

jobs:
  observe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Neon driver
        run: python -m pip install -r requirements-v22.txt
      - name: Run live 15-minute Brain cycle
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          V22_LIVE_HTTP_TIMEOUT_SECONDS: "5"
        run: python scripts/v22_github_cycle.py --cycle 15m --workflow-name "V22 15-Minute Free Validation"
